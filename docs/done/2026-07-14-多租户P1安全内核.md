# TODO — P1 多租户地基(最高危:动线上鉴权/路由,慢工细活)

来源:权哥 2026-07-14「A / 开搭 P1」。依据 docs/architecture/多租户地基-每个用户一个神枢.md。
铁律:切错=跨用户泄露/越权跑命令。先本地测透隔离,再碰线上。用量红线不变。

## 第一步:盘清鉴权面(只读,分类不能漏)
- [ ] 逐条列出「主人私密」路由块的每条路由,标 system_only / instance_owner
- [ ] capabilities.mjs 每个能力标 system_only / instance_owner(exec/全局config/跨用户stats=system)
- [ ] authOK 现状 + WS 票据流(P1 先把 WS 留 system_only)

## 第二步:设计可信身份握手(防伪造角色头)
- [ ] 顶层 worker:校验身份 → 剥掉客户端可能伪造的 X-Nexus-Role → 重设可信 role
- [ ] DO 只经 worker 可达,故信 worker 设的 role;绝不信客户端原始 role 头

## 第三步:实现(小步,每步可测)
- [ ] 顶层路由:owner token→系统实例(不变);X-Nexus-Uid→u:uid 实例;匿名→need_register
- [ ] DO authOK 角色化:system=全权;instance=只对自己 instance_owner 能力放行
- [ ] capabilities 两级 + resolveCapability 按角色
- [ ] 私密路由块按分类放行/拒绝
- [ ] 前端:所有请求带 X-Nexus-Uid

## 第四步:隔离测试(硬性,过了才算数)
- [ ] 两个 uid 数据互不可见(soul/记忆/私语各自独立)
- [ ] 实例主人碰不到 exec、烧不到系统 CF AI、看不到别人名单
- [ ] 系统主人(权哥)体验不变、数据零迁移
- [ ] build + selftest + sync-ui + 无头冒烟
- [ ] draft PR + 归档

## 红线
- exec 永远只系统主人;普通用户绝不烧权哥 CF AI;引擎词根表零改;/api/confirm 保留;
  跨用户物理隔离,任何接口不得据用户可控输入取别人数据

---
## 进度(2026-07-14)P1 第一刀已完成 → PR #42(已合并)
✅ 安全内核:tenancy.mjs(resolveIdentity 身份→角色+实例)+ capabilities 两级 tier + 15 条越权/隔离测试。
   纯逻辑、零线上改动、104 passed。最危险的判断已测死。

## P1 剩余(下一程专注做,动线上鉴权/路由,须重验)
- [ ] 顶层 worker 入口接 resolveIdentity:按角色选 DO(env 开关 MULTITENANT 默认关,预览验证后再开)
      · 剥掉客户端可能伪造的角色头,重设可信 role 转发给 DO
- [ ] DO 内 authOK 角色化:system=全权;instance=只放行 tier==='instance'
- [ ] 私密路由块按 tier 放行/拒绝(system_only 路由挡住实例主人)
- [ ] 实例对话走用户自己的 key(callGateway),绝不烧权哥 CF/gateway;造像造声暂 system_only
- [ ] 前端所有请求带 X-Nexus-Uid
- [ ] 隔离冒烟:两 uid 各自独立、实例主人碰不到 exec/系统资源、主人体验不变
- [ ] 开关灰度 → 预览验证 → 上线

## 交接提示
- 开关默认关 = 合了也不改线上,安全。开之前务必先在预览用两个 uid 实测隔离。
- 现有权哥数据在 quan-shenshu-nexus 实例,系统主人恒路由到它,零迁移。
