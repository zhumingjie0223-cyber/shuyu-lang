# TODO — 打磨"自带 key 驱动大模型"入口流程

来源:权哥 2026-07-14。产品模型=人人自带 API+key 驱动大模型,费用各付各的。
不做限流;把"进来→填 API/key→直接能用"的入口做顺、做稳。用量红线不变。

## 第一步:走查(只读,列卡点)
- [ ] 前端:注册页「自带大脑」表单(index.html)——引导文案、必填/可选、字段提示
- [ ] 前端:doRegister / prefillReg / openApiEditor 流程——填完去哪、失败怎样
- [ ] 后端:/register 存 api_url/key/model 逻辑
- [ ] 后端:/pubtalk → callGateway 调用——超时、连不上、key 错、返回异常时的报错文案
- [ ] 汇总卡点清单(哪里糊/哪里没提示/哪里报错不友好)

## 第二步:改(逐条打磨)
- [ ] 引导更明确(自带 key 是主路径的话,弱化"可选"的模糊)
- [ ] 校验:填了地址没填 key(或反之)要即时提示;格式明显不对给提示
- [ ] 报错友好:连不上/超时/401 key错/网关返回异常,各给一句人话
- [ ] 不破坏现有:内置大脑兜底路径保留(没填 key 仍可用内置)

## 第三步:验证 + 交付
- [ ] 构建 + selftest + sync-ui 双副本
- [ ] 无头浏览器截图入口流程(填 key 前/后)
- [ ] 提交 + 推送 + draft PR
- [ ] 归档 TODO 到 docs/done/

## 红线
- /api/confirm 保留;引擎词根表零改;鉴权硬门不削弱;公共用户不碰主人隐私

---
## 总结(2026-07-14)已完成 → Black-God PR #39(已合并)
产品模型=人人自带 API+key 驱动大模型。走查出三卡点并全修:
1. 消除"可选/必填"矛盾:API 字段从折叠"可选"改为主路径内联,去掉误导的"内置大脑"提示
2. 入口内嵌🔍识别模型(probeRegModels):填 url+key 一点即联网识别+验证网关通不通
3. doRegister 分字段校验;后端加公开 /probe-models(只用调用方自己网关,不回退主人配置,防 SSRF/key泄露)
验证:build + selftest 89 passed + sync-ui 双副本 + 无头截图。红线未碰。
