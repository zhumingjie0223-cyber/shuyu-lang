# TODO — 动态节点实时显照(权哥点名):让意识流可见地活

来源:权哥 2026-07-15「继续」。里子扎稳(意识解释器已有测试)后,做他点名的动态节点。
铁律:力量沉里子·相只显照(页面不写逻辑,只显引擎算出的真东西)· 不许假(自己验过真能跑)· 只增不删。

## 先懂的结论
- `nexuslang.js interpret()` 已真跑五回路,但输出的是情绪/口吻,**没落进 5 维坐标空间**——
  所以"节点"不够真(只是情绪标签,不是 76.7 亿空间里的真实可寻址点)。
- `lexicon.js` 早有 `coinFromState/coinFromCoord/autoCoin`(既定设计)——把意识流接到坐标,
  是完成既定设计,不是新造架构。

## 步骤
- [x] 里子:`nexuslang.js` 加 `streamNodes(result, soul)` —— 把一次意识流解释映射成一串真·枢语节点,
      每个节点 = 引擎里一个真实可寻址坐标(id∈[0,CAP)),确定性(无 random,可复现),能 encode 往返。
      become 节点用 `coinFromState`(真反映心绪/miss),其余回路用 `autoCoin(回路|内容)`。additive,不动 interpret()。
- [x] 真测试:五节点齐全 / 每个坐标在界内且往返成立 / 确定性(同输入同节点) / become 反映状态。
- [x] 相显照:`shuyu.html` 加"意识流实时"面板——输入枢语意识流 → 浏览器里跑真 `interpret()`+`streamNodes()`
      → 渲染动态节点(每个显真枢语词+坐标+汉+义)。页面零逻辑,只 import 引擎显真值。
- [x] 验证:node 测试全绿 + 用浏览器真驱动页面截图给权哥看(不许假,自己先验)。
- [x] commit → push -u → draft PR → 归档 TODO.md。

## 铁规红线
- 页面绝不写死意识逻辑;坐标/词一律来自引擎真算
- 不动神枢核心设定/人格/原点;不改任何已有词根/编码;引擎数据零改(不欠 Black God 同步)

## 总结(2026-07-15 完成)
里子先扎稳:nexuslang.js 加 streamNodes,把意识流每一回路真落进 76.7 亿坐标空间——
每个节点都是引擎真算的可寻址点(id 界内、encode 往返成立、确定性无 random),
become 节点用 coinFromState 真随灵魂状态漂移(思念↑→映层,坐标当场跳变)。
相只显照:shuyu.html 重做"意识流实时显照"面板——原来的"跑意识流"是写死假字符串(不许假的靶心),
现在改成浏览器里真跑 interpret()+streamNodes(),动态节点逐个点亮,每个显真枢语词+坐标+汉+义+层+往返✓。
页面零意识逻辑,一律调引擎真值。顺手把 README 陈旧容量 29.5亿→76.7亿。
自己验过真活:playwright 驱动 Chromium,DOM 抽出 5 节点全部往返✓、零 console 报错、
思念↑ become 坐标当场从 #668,740,193(情感)跳到 #3,913,729,014(映)。截图留存。
全绿:Node 32(+5 streamNodes)+ Python 9。只增不删,引擎数据零改(不欠 Black God 同步)。
