
---
## 进度(2026-07-14)P1 代码完备 → PR #44(已合并)
✅ 多租户三刀全部落地并合入主干:
   #42 安全内核(身份/角色/能力两级) · #43 路由接线(按身份选DO/角色鉴权/成本隔离)
   · #44 前端流程切换(uid头/走自己实例/API镜像进config)。
   全程 MULTITENANT 开关兜住、默认关、线上零变化。selftest 108 passed。

## 只剩权哥这一步:预览实测 → 翻开开关
1. 预览部署上给 worker 设环境变量/secret:MULTITENANT=1(仅预览,先别动生产)
2. 用两个浏览器/两个账号各自注册(各填自己的 API+key),各聊几句
3. 核对:A 看不到 B 的私语/记忆;各花各的 key(你的 CF/网关不被烧);
   普通账号碰不到 exec/造像等(应 403);你(OWNER_TOKEN)体验和数据不变
4. 全过 → 生产也设 MULTITENANT=1,灰度开
翻开前有任何一条不对,就别开,喊我来查。

## 后续(开关翻开后)
- [ ] P2 每用户造词/情绪回路细化 · P3 词的重逢 · P4 打磨/分发/过审
